global:
  scrape_interval: 5s

scrape_configs:
  - job_name: 'user-service-spring-boot-app'
    metrics_path: '/api/actuator/prometheus'
    static_configs:
      - targets: ['user-service:8080'] #user-service - service name in docker compose