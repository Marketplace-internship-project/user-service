<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
 http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.6.xsd">

    <!-- ChangeSet: create users table -->
    <changeSet id="001-create-users" author="auto-generated">
        <preConditions onFail="MARK_RAN">
            <not>
                <tableExists tableName="users"/>
            </not>
        </preConditions>

        <createTable tableName="users">
            <column name="id" type="uuid">
                <constraints primaryKey="true" nullable="false"/>
            </column>

            <column name="name" type="varchar(255)">
                <constraints nullable="false"/>
            </column>

            <column name="surname" type="varchar(255)"/>

            <column name="birth_date" type="date"/>

            <column name="email" type="varchar(255)">
                <constraints nullable="false" unique="true"/>
            </column>
        </createTable>

        <!-- Optional: explicit unique constraint (redundant with constraints unique on column) -->
        <addUniqueConstraint tableName="users" columnNames="email" constraintName="uc_users_email"/>
    </changeSet>

    <!-- ChangeSet: create card_info table and FK to users -->
    <changeSet id="002-create-card-info" author="auto-generated">
        <preConditions onFail="MARK_RAN">
            <not>
                <tableExists tableName="card_info"/>
            </not>
        </preConditions>

        <createTable tableName="card_info">
            <column name="id" type="uuid">
                <constraints primaryKey="true" nullable="false"/>
            </column>

            <column name="user_id" type="uuid">
                <constraints nullable="false"/>
            </column>

            <column name="number" type="varchar(64)">
                <constraints nullable="false" unique="true"/>
            </column>

            <column name="holder" type="varchar(255)">
                <constraints nullable="false"/>
            </column>

            <column name="expiration_date" type="date">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <!-- Add foreign key with ON DELETE CASCADE to reflect JPA cascade behavior -->
        <addForeignKeyConstraint baseTableName="card_info"
                                 baseColumnNames="user_id"
                                 constraintName="fk_card_info_user"
                                 referencedTableName="users"
                                 referencedColumnNames="id"
                                 onDelete="CASCADE"/>

        <!-- Explicit unique constraint on card number -->
        <addUniqueConstraint tableName="card_info" columnNames="number" constraintName="uc_card_info_number"/>
    </changeSet>

    <!-- ChangeSet: add index on users.email -->
    <changeSet id="003-create-users-email-index" author="auto-generated">
        <preConditions onFail="MARK_RAN">
            <not>
                <indexExists indexName="idx_users_email" tableName="users"/>
            </not>
        </preConditions>

        <createIndex indexName="idx_users_email" tableName="users">
            <column name="email"/>
        </createIndex>
    </changeSet>

</databaseChangeLog>
